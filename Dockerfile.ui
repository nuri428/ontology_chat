# UI 서버용 Dockerfile
FROM python:3.12-slim

# 작업 디렉토리 설정
WORKDIR /app

# 시스템 패키지 업데이트 및 필요한 패키지 설치
RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

# uv 설치
RUN pip install uv

# 프로젝트 파일 복사
COPY pyproject.toml uv.lock ./
COPY ui/ ./ui/

# 의존성 설치
RUN uv sync --frozen

# 포트 노출
EXPOSE 8501

# 환경 변수 설정
ENV PYTHONPATH=/app
ENV API_BASE_URL=http://api:8000

# Streamlit UI 서버 실행
CMD ["uv", "run", "streamlit", "run", "ui/main.py", "--server.address", "0.0.0.0", "--server.port", "8501"]
